<%= labelled_form_for @project_author,
                   :as => :project_author,
                   :url => { :controller => 'author_adder', :action => 'edit', :project_id => @project.id },
                   :remote => true, :method => 'post' do |f| %>

<%= error_messages_for 'wiki' %>


<div class="box tabular">
<p><%= f.select :user_id, options_from_collection_for_select(User.all, "id", "name", @project_author.user_id), :size => 60, :required => true %>
</p>
</div>

<%= submit_tag((@wiki.nil? || @wiki.new_record?) ? l(:button_create) : l(:button_save)) %>
<% end %>
